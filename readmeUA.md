# Mass Telnet Command Executor
## Це програмне забезпечення є власністю компанії. Несанкціоноване використання або розповсюдження заборонено.

CLI-скрипт для масового виконання команд на комутаторах через Telnet з підтримкою:

- Підключення до декількох портів (`IP` або `IP:PORT`)
- Паралельного виконання команд з конфігурованою кількістю потоків
- Таймаутів на з'єднання та виконання команд
- Кількості спроб повторного підключення
- Логування в окрему папку `logs/` (`log_таймштамп.txt`, `success.txt`, `fail.txt`)
- Підсумків у кінці з часом виконання (мсек)
- Прогрес під час виконання
- **НОВЕ: Підтримка резервного копіювання конфігурацій через TFTP з файлами з таймштампами**

---

## 📂 Структура файлів

```
run.py                  # Основний скрипт
download_config.py      # Скрипт резервного копіювання через TFTP
config.json             # Конфігураційний файл
switches.txt            # Список комутаторів (IP або IP:PORT)
commands.txt            # Команди для виконання
credentials.json        # Логін/пароль
logs/                   # Папка з логами з файлами з таймштампами
```

---

## ⚙️ Конфігураційний файл `config.json`

```json
{
  "default_port": 23,
  "parallel_connections": 5,
  "connection_timeout": 10,
  "command_timeout": 5,
  "max_retries": 2,
  "tftp_server": "10.1.11.97"
}
```

- `default_port` — порт Telnet за замовчуванням
- `parallel_connections` — кількість одночасних підключень
- `connection_timeout` — таймаут підключення до свіча (секунди)
- `command_timeout` — таймаут на виконання команди (секунди)
- `max_retries` — кількість повторних спроб при невдалому з'єднанні
- `tftp_server` — IP-адреса TFTP-сервера для резервного копіювання конфігурацій

---

## 📄 Формати файлів

**switches.txt**

```
192.168.1.10
192.168.1.11:2323
192.168.1.12:24
```

**commands.txt**

```
help
save
```

**credentials.json**

```json
{
  "username": "admin",
  "password": "your_pass"
}
```

---

## 🖥️ Запуск

**Виконання команд на комутаторах:**
```bash
python run.py
```

**Резервне копіювання конфігурацій через TFTP:**
```bash
python download_config.py
```

- Скрипт автоматично створює папку `logs/` і файли логів з таймштампами (наприклад, `log_2026-01-01_17-03-28.txt`, `success.txt`, `fail.txt`)
- Під час виконання показує прогрес у терміналі
- Після завершення показує підсумок:
  - Загальна кількість комутаторів
  - Кількість успішних і невдалих
  - Час виконання (мсек)

---

## � Резервне копіювання через TFTP

Скрипт `download_config.py` автоматично створює резервні копії конфігурацій комутаторів на TFTP-сервер:

- Підключається до кожного комутатора зі списку `switches.txt`
- Виконує команду `upload cfg_toTFTP <tftp_server> dest_file <IP>_<PORT>_<TIMESTAMP>.cfg`
- Очікує підтвердження "Success" від комутатора
- У разі невдачі пробує альтернативний формат команди
- Файли зберігаються на TFTP-сервері у форматі: `10.2.20.5_234_2026-01-01_17-03-28.cfg`

**Приклад виводу:**
```
[1/4] 10.2.20.6:234 (try 1)
Executing: upload cfg_toTFTP 10.1.11.97 dest_file 10.2.20.6_234_2026-01-01_17-03-28.cfg
Upload successful: 10.2.20.6_234_2026-01-01_17-03-28.cfg
✓ SUCCESS 10.2.20.6:234
```

---

## �📊 Логування

- `logs/log_YYYY-MM-DD_HH-MM-SS.txt` — детальні логи з таймштампом у назві файлу, містять IP, порт, результат та вивід команд
- `logs/success.txt` — список успішних підключень з датою запуску
- `logs/fail.txt` — список невдалих підключень з датою запуску
- Лог-файли створюються з таймштампом для кожного запуску скрипта, зберігаючи історію всіх виконань

---

## 🔹 Особливості

- Підтримка IP або IP:PORT
- Паралельні підключення з конфігурованою кількістю потоків
- Таймаути на з'єднання і виконання команд
- Кількість спроб для невдалих підключень
- Прогрес виконання в терміналі
- Підсумок з часом виконання в мілісекундах
- Модульна структура файлів для легкого редагування: `switches.txt`, `commands.txt`, `credentials.json`
- **Резервне копіювання конфігурацій через TFTP з автоматичним повторенням при невдачі**
- **Резервні копії з назвами IP, порт та таймштамп для легкої ідентифікації та відстеження версій**
- **Лог-файли з таймштампами зберігають історію виконань**

---

## 📌 Рекомендації

- Кожен запуск скрипта створює новий лог-файл з таймштампом, зберігаючи повну історію виконань
- Резервні копії конфігурацій містять таймштампи, що дозволяє відстежувати версії та відновлювати конфігурації
- Для великих мереж рекомендовано збільшити `parallel_connections`, але не перевантажуйте комутатори
- Старі лог-файли можна безпечно архівувати або видаляти без впливу на нові запуски

---

## ⚡ Приклад запуску для провайдера

- Провайдер має 1000 комутаторів → один запуск скрипта замінює години ручної роботи
- Після завершення легко переглянути:
  - Невдалі підключення (`fail.txt`)
  - Успішні підключення (`success.txt`)
